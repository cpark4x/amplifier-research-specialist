# Design & Implementation Plans (Historical Archive)

These are dated design and implementation documents from the project's development.
They capture the thinking and decisions behind each feature as it was built.

**These are historical records, not current documentation.**

Known staleness:
- File paths reference `specialists/{name}/index.md` — the project now uses `agents/{name}.md`
- Some files reference a `scripts/` directory that was never created
- Epics 05 (Design), 06 (Demo Generator), 07 (Presentation Builder), and 11 (Platform Integrations) were removed from scope in March 2026

**For current project state:**
- `docs/BACKLOG.md` — what's shipped, what's next
- `agents/*.md` — current agent definitions